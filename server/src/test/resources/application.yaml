spring:
  datasource:
    url: jdbc:h2:mem:httt-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  data:
    redis:
      host: localhost
      port: 6379
      repositories:
        enabled: false
  ai:
    openai:
      api-key: test-openai-key
  mail:
    host: localhost
    port: 1025

app:
  security:
    jwt-secret: test-secret-key-minimum-32-chars-123
    access-token-duration: PT30M
    refresh-token-duration: P7D
